# Cursor — éditeur (ne jamais versionner)
.cursor/

# RCT (scans) / données locales — ne jamais versionner
source/

# Artefacts locaux, extraits de test
output/

# Procédure de lancement locale (usage personnel)
PROCEDURE_LANCEMENT.md

# Guide rédaction questions (usage local / IA)
docs/REDIGER-QUESTIONS-RCT.md

# OS & éditeurs
.DS_Store
Thumbs.db
*.log
*.swp
*~

# Secrets
.env
.env.local
.env.*.local
