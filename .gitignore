# Cursor — éditeur (ne jamais versionner)
.cursor/

# Bible / scans / données locales — ne jamais versionner
source/

# Artefacts locaux, extraits de test
output/

# OS & éditeurs
.DS_Store
Thumbs.db
*.log
*.swp
*~

# Secrets
.env
.env.local
.env.*.local
