# Cursor — éditeur (ne jamais versionner)
.cursor/

# Données locales lourdes ou brouillons — ne pas versionner (scans RCT : docs/rct-img/)
source/

# Artefacts locaux, extraits de test
output/

# Procédure de lancement locale (usage personnel)
PROCEDURE_LANCEMENT.md

# Guides rédaction et évolutions (usage local / IA)
docs/REDIGER-QUESTIONS-RCT.md
docs/REDACTION_REPONSES_CARTES.md
docs/EVOLUTIONS.md

# Suivi audit local (non versionné)
docs/AUDIT_RCT.md
docs/HOWTO_BACKUP_ECRASEMENT.md
scripts/list-rct-questions.mjs

# Réponses quizz / positionnement / outils (travail local)
reponses-*.txt
reponses-*.md
*.pdf

# Dépendances Node (tests locaux — jamais versionner)
node_modules/
docs/node_modules/

# Python
__pycache__/
*.pyc

# OS & éditeurs
.DS_Store
Thumbs.db
*.log
*.swp
*~

# Secrets
.env
.env.local
.env.*.local
